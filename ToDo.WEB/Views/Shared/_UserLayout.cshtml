@{
    ViewData["Title"] = "_UserLayout";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<nav class="navbar navbar-expand-lg text-white bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand text-light" href="@Url.Action("index","home")">TODO</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active text-light" aria-current="page" href="@Url.Action("logout","auth")">Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

@RenderBody()

@await RenderSectionAsync("Scripts", required: false)